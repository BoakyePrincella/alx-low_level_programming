#include "main.h"

/**
 * _isupper - checks the uppercase character
 * @c:function parameter
 * Return:1 for uppercase
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}
